{
  "name": "learner",
  "private": true,
  "scripts": {
    "dev": "concurrently -n worker,web -c cyan,magenta \"npm run dev --prefix worker\" \"npm run dev --prefix web\"",
    "build": "npm install --prefix web && npm run build --prefix web",
    "db:apply:remote": "npm run db:apply:remote --prefix worker",
    "deploy": "npm run build --prefix web && npm run deploy --prefix worker",
    "db:local": "npm run db:local --prefix worker"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
